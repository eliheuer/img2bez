# Build artifacts
target
**/*.rs.bk
*.pdb

# macOS
.DS_Store

# Editor / assistant local configuration
.claude/

# Debug comparison renders img2bez emits next to its output
*_comparison.png
*_diff.png

# Eval-harness local state (regenerable; reference.ufo is a local symlink)
.venv-eval-harness/
eval-harness/work/
eval-harness/stress-work/
eval-harness/runs/
eval-harness/corpus-work/
eval-harness/label-work/
eval-harness/__pycache__/
eval-harness/reference.ufo

# Trace-collection logs + private training data — never commit.
# Decision/trace logs are JSONL; derived datasets stay in a private repo.
*.jsonl
/dataset/
eval-harness/dataset/
